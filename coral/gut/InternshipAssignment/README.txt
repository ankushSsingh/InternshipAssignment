#The Database server is not getting connected. I don't know why. I have written the code correctly still I have no idea.


# Server is built on NodeJS - Express Architecture with MySQL as database.

#To run the app:
npm install
npm start

#Server will be launched at localhost:3000



